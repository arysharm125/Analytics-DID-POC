# Docker Compose configuration for load testing
# This extends the dev setup with Locust for performance benchmarking

networks:
  default:
    name: didsvc-loadtest

services:
  mongodb:
    image: mongo:6.0
    container_name: didsvc-loadtest-mongodb
    restart: unless-stopped
    volumes:
      - mongodb_loadtest_data:/data/db

  vault:
    image: hashicorp/vault:1.15
    container_name: didsvc-loadtest-vault
    restart: unless-stopped
    cap_add:
      - IPC_LOCK
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: "loadtest-root-token"
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"

  vault-init:
    image: hashicorp/vault:1.15
    container_name: didsvc-loadtest-vault-init
    depends_on:
      - vault
    environment:
      VAULT_ADDR: "http://vault:8200"
      VAULT_TOKEN: "loadtest-root-token"
    entrypoint: /bin/sh
    command: -c "sleep 5 && vault kv put secret/mongo connection_string=mongodb://mongodb:27017/ database_name=loadtest_did_service"
    restart: "no"

  api:
    container_name: didsvc-loadtest-api
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    command: uvicorn app.main:app --host 0.0.0.0 --port 8434 --workers 1 --no-access-log
    env_file:
      - ../.env.loadtest
    restart: unless-stopped
    depends_on:
      mongodb:
        condition: service_started
      vault:
        condition: service_started
      vault-init:
        condition: service_completed_successfully
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G

  nginx:
    image: nginx:1.29.6-alpine3.23-slim
    container_name: didsvc-loadtest-nginx
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.loadtest.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
    ports:
      - "8432:80"

  locust:
    image: locustio/locust:2.24.0
    container_name: didsvc-loadtest-locust
    restart: unless-stopped
    ports:
      - "8433:8089"
    volumes:
      - ../benchmarks:/mnt/benchmarks
    working_dir: /mnt/benchmarks/locustfiles
    environment:
      - ADVISORY_API_TOKEN=test-advisory-token
      - EPDW_API_TOKEN=test-epdw-token
    command: >
      -f advisory_scenarios.py
      --host http://nginx
      --web-host 0.0.0.0
    depends_on:
      - nginx

volumes:
  mongodb_loadtest_data:
